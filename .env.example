##################################
# Braiins Pool
##################################

# Development option: paste a read-only Braiins Pool API token here.
BRAIINS_POOL_TOKEN=

# Preferred production-style option: mount a token file and set this path.
# For Docker Compose, place the token in secrets/braiins_pool_token and set:
# BRAIINS_POOL_TOKEN_FILE=/run/secrets/braiins_pool_token
BRAIINS_POOL_TOKEN_FILE=

BRAIINS_POOL_COIN=btc

##################################
# Exporter
##################################

BRAIINS_POOL_API_BASE_URL=

##################################
# Polling
##################################

BRAIINS_POOL_POLL_INTERVAL=10s
BRAIINS_POOL_TIMEOUT=10s
BRAIINS_POOL_WORKER_METRICS_ENABLED=true
BRAIINS_POOL_MAX_WORKERS=100
BRAIINS_POOL_REWARDS_ENABLED=true
BRAIINS_POOL_PAYOUTS_ENABLED=true
BRAIINS_POOL_HISTORY_DAYS=7

##################################
# Optional Avalon miner exporter
##################################

# Start with:
# docker compose -f compose.yaml -f compose.miner.yaml --profile miner up -d
#
# These are local development placeholders for six AvalonMiner 1047 targets.
# Override them in your uncommitted .env with real local IPs or hostnames.
AVALON_IPS=avalon1047-01.local,avalon1047-02.local,avalon1047-03.local,avalon1047-04.local,avalon1047-05.local,avalon1047-06.local
AVALON_PORT=4028

# Upstream avalonhome-prometheus-exporter variable names.
UPDATE_INTERVAL=15
EXPORTER_PORT=9100
EXPORT_CHIP_METRICS=false
MINER_TIMEOUT=5.0
ENABLE_DEBUG_ENDPOINT=false
AVALON_LOG_LEVEL=INFO
